name: Schedule action

#on:
#  schedule:
#    - cron: '*/10 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - name: Run test
        uses: actions/checkout@v1
        with:
          ref: feat/ci-scheduler
      - run: python --version
